# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL server hostname (use 'db' for Docker Compose service name)
POSTGRES_SERVER=db
# PostgreSQL port
POSTGRES_PORT=5432
# Database name
POSTGRES_DB=app
# Database user
POSTGRES_USER=postgres
# Database password (CHANGE THIS in production!)
POSTGRES_PASSWORD=changethis123

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Redis server hostname (use 'redis' for Docker Compose service name)
REDIS_HOST=redis
# Redis port
REDIS_PORT=6379
# Redis database number
REDIS_DB=0
# Redis password (optional, leave empty for no password)
REDIS_PASSWORD=
# Redis max connections
REDIS_MAX_CONNECTIONS=20

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
# Domain for the application
DOMAIN=localhost
# Frontend host URL
FRONTEND_HOST=http://localhost
# Environment: local, staging, or production
ENVIRONMENT=local
# CORS origins (comma-separated list)
BACKEND_CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:5173
# Secret key for JWT tokens (CHANGE THIS! Must be at least 32 characters)
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
# Access token expiration in minutes (default: 8 days)
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# =============================================================================
# FIRST SUPERUSER (Admin Account)
# =============================================================================
# Email for the first superuser
FIRST_SUPERUSER=admin@example.com
# Password for the first superuser (CHANGE THIS!)
FIRST_SUPERUSER_PASSWORD=changethis123

# =============================================================================
# EMAIL CONFIGURATION (SMTP)
# =============================================================================
# SMTP server hostname (leave empty to disable emails)
SMTP_HOST=
# SMTP server port
SMTP_PORT=587
# SMTP username
SMTP_USER=
# SMTP password
SMTP_PASSWORD=
# Use TLS encryption
SMTP_TLS=true
# Use SSL encryption
SMTP_SSL=false
# Email address to send from
EMAILS_FROM_EMAIL=noreply@example.com
# Name to send emails from (defaults to PROJECT_NAME if not set)
EMAILS_FROM_NAME=

# =============================================================================
# PROJECT CONFIGURATION
# =============================================================================
# Stack name for Docker Compose
STACK_NAME=pyllren
# Project name
PROJECT_NAME=PillREN

# =============================================================================
# DOCKER IMAGES (for building)
# =============================================================================
# Backend Docker image name
DOCKER_IMAGE_BACKEND=pyllren-backend
# Frontend Docker image name
DOCKER_IMAGE_FRONTEND=pyllren-frontend
# Image tag
TAG=latest

# =============================================================================
# MONITORING & LOGGING (Optional)
# =============================================================================
# Sentry DSN for error tracking (optional, leave empty to disable)
SENTRY_DSN=

# =============================================================================
# NOTES
# =============================================================================
# 1. Copy this file to .env and update the values
# 2. Never commit .env file to version control
# 3. Change all "changethis" passwords in production
# 4. Generate a secure SECRET_KEY: openssl rand -hex 32

